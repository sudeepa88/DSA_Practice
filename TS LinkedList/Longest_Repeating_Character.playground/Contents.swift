import Cocoa

//var greeting = "Hello, playground"



func characterReplacement(_ s: String) {
   var sap = "AABABBA"
    
    
    var maxCount = 0
    var minCount = 0
    for i in sap {
        var ap = i
       for i in sap {
           if ap == i {
               maxCount = maxCount + 1
           }
        }
    }
}
